import "../scss/header.scss";

const Headers = () => {
  return (
    <div className='main-nav'>
      <nav>
        <a href='/result'>Result</a>
        <a href='/things-to-do'>Things to do</a>
        <a href='/things-to-do'>Alert</a>
        <a href='/things-to-do'>HomePage</a>
      </nav>
    </div>
  );
};

export default Headers;
